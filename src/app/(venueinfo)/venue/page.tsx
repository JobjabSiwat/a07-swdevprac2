import CardPanel from '@/components/CardPanel';

export default function VenuePage() {
    return (
        <main
            style={{
                minHeight: '100vh',
                backgroundColor: '#f9fafb',
                paddingTop: '80px',
            }}
        >
            <CardPanel />
        </main>
    );
}